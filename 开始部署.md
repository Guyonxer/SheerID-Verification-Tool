# 🚀 开始部署 - 跟我一步步来

> **预计时间**: 10-15 分钟  
> **难度**: ⭐⭐ (简单)  
> **费用**: 完全免费

---

## 📋 准备工作

在开始之前，确保你有：
- ✅ GitHub 账号
- ✅ Git 已安装
- ✅ 网络连接正常

---

## 🎯 第一步：准备你的 GitHub 仓库

### 方式 A: Fork 原仓库（最简单）⭐

1. **打开原仓库**
   ```
   https://github.com/ThanhNguyxn/SheerID-Verification-Tool
   ```

2. **点击 Fork 按钮**
   - 在页面右上角找到 "Fork" 按钮
   - 点击后等待几秒钟
   - Fork 完成后会自动跳转到你的仓库

3. **克隆到本地**
   ```bash
   git clone https://github.com/你的用户名/SheerID-Verification-Tool.git
   cd SheerID-Verification-Tool
   ```

### 方式 B: 推送到新仓库

1. **创建新仓库**
   - 访问: https://github.com/new
   - 仓库名: `sheerid-tool` (或任意名称)
   - 设为 Public
   - **不要**勾选 "Initialize with README"
   - 点击 "Create repository"

2. **推送代码**
   ```bash
   cd SheerID-Verification-Tool
   git remote remove origin
   git remote add origin https://github.com/你的用户名/sheerid-tool.git
   git add .
   git commit -m "Initial commit with deployment configs"
   git push -u origin master
   ```

✅ **完成第一步！**

---

## 🚀 第二步：部署后端到 Render.com

### 1. 注册/登录 Render

**打开浏览器，访问：**
```
https://render.com
```

**点击右上角 "Get Started" 或 "Sign In"**

**选择 "Sign in with GitHub"**
- 授权 Render 访问你的 GitHub
- 选择允许访问你的仓库

### 2. 创建 Web Service

**在 Render Dashboard：**

1. 点击 **"New +"** 按钮（右上角）
2. 选择 **"Web Service"**
3. 找到你的仓库 `SheerID-Verification-Tool` 或 `sheerid-tool`
4. 点击 **"Connect"**

### 3. 配置服务

**填写以下信息：**

| 字段 | 值 |
|------|-----|
| **Name** | `sheerid-api` (或任意名称) |
| **Region** | `Singapore` (或离你最近的) |
| **Branch** | `master` |
| **Root Directory** | `auto-verify-tool` ⚠️ 重要！ |
| **Environment** | `Docker` |
| **Docker Command** | (留空) |
| **Instance Type** | `Free` |

**点击 "Create Web Service"**

### 4. 等待部署

- 部署需要 **5-10 分钟**
- 可以在 **Logs** 标签页查看进度
- 看到 "Your service is live" 表示成功

### 5. 复制后端 URL

部署完成后，你会看到类似这样的 URL：
```
https://sheerid-api-abc123.onrender.com
```

**复制这个 URL！** 我们下一步需要用到。

✅ **完成第二步！**

---

## 🔧 第三步：更新前端配置

### 1. 编辑 index.html

**打开文件：** `SheerID-Verification-Tool/index.html`

**找到这一行（大约在第 300 行左右）：**
```javascript
const BACKEND_URL = 'https://sheerid-api-v2-d5gdbxa5hjd2baak.japaneast-01.azurewebsites.net';
```

**替换为你的后端 URL：**
```javascript
const BACKEND_URL = 'https://sheerid-api-abc123.onrender.com';
```

### 2. 提交更改

```bash
git add index.html
git commit -m "Update backend URL"
git push
```

✅ **完成第三步！**

---

## 🌐 第四步：部署前端

### 方式 A: Render Static Site（推荐）

**在 Render Dashboard：**

1. 点击 **"New +"** → **"Static Site"**
2. 选择同一个仓库
3. 配置：
   - **Name**: `sheerid-frontend`
   - **Branch**: `master`
   - **Build Command**: `echo "No build needed"`
   - **Publish Directory**: `.` (一个点)
4. 点击 **"Create Static Site"**
5. 等待 1-2 分钟部署完成

**你的前端 URL：**
```
https://sheerid-frontend-xyz.onrender.com
```

### 方式 B: Vercel（更快）

```bash
# 安装 Vercel CLI
npm install -g vercel

# 部署
cd SheerID-Verification-Tool
vercel --prod
```

按照提示操作，几分钟后就能得到 URL。

✅ **完成第四步！**

---

## 🎉 第五步：测试部署

### 1. 访问前端

打开浏览器，访问你的前端 URL：
```
https://sheerid-frontend-xyz.onrender.com
```

### 2. 测试功能

1. 在输入框输入一个测试 URL（或使用示例）
2. 选择服务类型（Gemini/ChatGPT/Spotify）
3. 点击 **"VERIFY NOW"**
4. 查看实时日志

### 3. 检查后端

运行检查工具：
```bash
node check-deployment.js
```

或手动访问：
```
https://sheerid-api-abc123.onrender.com
```

应该看到：
```json
{"status":"ok","message":"SheerID Verification API is running"}
```

✅ **部署成功！** 🎊

---

## 📊 部署总结

你现在有：

| 服务 | URL | 状态 |
|------|-----|------|
| 后端 API | `https://sheerid-api-xxx.onrender.com` | ✅ 运行中 |
| 前端界面 | `https://sheerid-frontend-xxx.onrender.com` | ✅ 运行中 |

---

## ⚠️ 重要提示

### 免费套餐限制

- **休眠机制**: 15 分钟无活动后会休眠
- **唤醒时间**: 首次访问需要 30-60 秒
- **解决方案**: 使用 [UptimeRobot](https://uptimerobot.com) 定期 ping

### 保持唤醒

1. 注册 UptimeRobot（免费）
2. 添加监控：
   - URL: 你的后端 URL
   - 监控间隔: 5 分钟
3. 这样服务就不会休眠了

---

## 🔧 下一步优化

### 1. 配置自定义域名

**在 Render Dashboard：**
- Settings → Custom Domain
- 添加你的域名
- 配置 DNS 记录

### 2. 添加访问密码

编辑 `server.js`，添加简单的认证：
```javascript
app.use((req, res, next) => {
  const auth = req.headers.authorization;
  if (auth === 'Bearer your-secret-key') {
    next();
  } else {
    res.status(401).json({ error: 'Unauthorized' });
  }
});
```

### 3. 添加监控

- **Sentry**: 错误追踪
- **Google Analytics**: 访问统计
- **UptimeRobot**: 可用性监控

---

## 🆘 遇到问题？

### 常见问题

**Q1: 部署失败，显示 "Out of memory"**
```
原因: 内存不足
解决: Dockerfile 已优化，如仍失败考虑升级套餐
```

**Q2: 访问很慢或超时**
```
原因: 服务休眠中
解决: 等待 30-60 秒，或使用 UptimeRobot 保持唤醒
```

**Q3: CORS 错误**
```
原因: 前端域名未在白名单
解决: 编辑 server.js，添加你的前端域名到 CORS 配置
```

**Q4: 找不到 Root Directory**
```
原因: 路径配置错误
解决: 确保 Root Directory 设为 "auto-verify-tool"
```

### 获取帮助

- 📖 查看 [QUICK-DEPLOY.md](QUICK-DEPLOY.md)
- 📖 查看 [DEPLOYMENT.md](DEPLOYMENT.md)
- 🐛 提交 Issue: https://github.com/ThanhNguyxn/SheerID-Verification-Tool/issues

---

## ✅ 部署检查清单

完成后打勾：

- [ ] GitHub 仓库已准备
- [ ] 后端已部署到 Render
- [ ] 后端 URL 已复制
- [ ] index.html 已更新
- [ ] 代码已提交推送
- [ ] 前端已部署
- [ ] 测试功能正常
- [ ] (可选) 配置 UptimeRobot
- [ ] (可选) 添加自定义域名

---

## 🎊 恭喜！

你已经成功将 SheerID Verification Tool 部署到云端了！

**分享你的工具：**
```
🌐 https://sheerid-frontend-xyz.onrender.com
```

**下次更新代码：**
```bash
git add .
git commit -m "Update features"
git push
```

Render 会自动检测更改并重新部署！

---

**祝使用愉快！** 🚀

如果觉得有用，别忘了给项目点个 ⭐
